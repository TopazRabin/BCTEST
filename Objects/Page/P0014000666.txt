OBJECT Page 14000666 Pack Line Scanning Subform
{
  OBJECT-PROPERTIES
  {
    Date=06/28/17;
    Time=12:00:00 PM;
    Version List=SE0.54.10;
  }
  PROPERTIES
  {
    Editable=No;
    CaptionML=ENU=Pack Line Scanning Subform;
    SourceTable=Table14000702;
    PageType=ListPart;
    OnFindRecord=BEGIN
                   PackageLineTmp := Rec;
                   IF NOT PackageLineTmp.FIND(Which) THEN
                     EXIT(FALSE);
                   Rec := PackageLineTmp;
                   EXIT(TRUE);
                 END;

    OnNextRecord=BEGIN
                   PackageLineTmp := Rec;
                   CurrentSteps := PackageLineTmp.NEXT(Steps);
                   IF CurrentSteps <> 0 THEN
                     Rec := PackageLineTmp;
                   EXIT(CurrentSteps);
                 END;

    OnInsertRecord=BEGIN
                     EXIT(FALSE);
                   END;

    OnModifyRecord=BEGIN
                     EXIT(FALSE);
                   END;

    OnDeleteRecord=BEGIN
                     EXIT(FALSE);
                   END;

  }
  CONTROLS
  {
    { 1900000001;0;Container;
                ContainerType=ContentArea }

    { 1   ;1   ;Group     ;
                GroupType=Repeater }

    { 2   ;2   ;Field     ;
                SourceExpr=Type }

    { 4   ;2   ;Field     ;
                SourceExpr="No." }

    { 6   ;2   ;Field     ;
                SourceExpr="Variant Code";
                Visible=FALSE }

    { 22  ;2   ;Field     ;
                SourceExpr="Selected Source ID";
                Visible=FALSE }

    { 8   ;2   ;Field     ;
                SourceExpr="Serial No.";
                Visible=FALSE }

    { 10  ;2   ;Field     ;
                SourceExpr="Lot No.";
                Visible=FALSE }

    { 18  ;2   ;Field     ;
                SourceExpr="Warranty Date";
                Visible=FALSE }

    { 20  ;2   ;Field     ;
                SourceExpr="Expiration Date";
                Visible=FALSE }

    { 12  ;2   ;Field     ;
                SourceExpr=Description }

    { 14  ;2   ;Field     ;
                SourceExpr=Quantity }

    { 16  ;2   ;Field     ;
                SourceExpr="Unit of Measure Code" }

  }
  CODE
  {
    VAR
      PackageLineTmp@1240030000 : TEMPORARY Record 14000702;
      CurrentSteps@1240030002 : Integer;

    PROCEDURE LoadPackageLines@1(VAR PackageLine@1240030000 : Record 14000702;PackingControl@1240030001 : Record 14000717);
    BEGIN
      PackageLineTmp.RESET;
      PackageLineTmp.DELETEALL;
      IF PackageLine.FIND('-') THEN
        REPEAT
          PackageLineTmp := PackageLine;
          PackageLineTmp.INSERT;
        UNTIL PackageLine.NEXT = 0;

      PackageLineTmp.RESET;
      PackageLineTmp.SETRANGE(Type,PackingControl."Input Type");
      PackageLineTmp.SETRANGE("No.",PackingControl."Input No.");
      IF PackingControl."Input Variant Code" <> '' THEN
        PackageLineTmp.SETRANGE("Variant Code",PackingControl."Input Variant Code");
      IF PackingControl."Input Serial Number" <> '' THEN
        PackageLineTmp.SETRANGE("Serial No.",PackingControl."Input Serial Number");
      IF PackingControl."Input Lot Number" <> '' THEN
        PackageLineTmp.SETRANGE("Lot No.",PackingControl."Input Lot Number");
      IF NOT PackageLineTmp.FIND('-') THEN BEGIN
        PackageLineTmp.RESET;
        IF NOT PackageLineTmp.FIND('+') THEN
          ;
      END;
      PackageLineTmp.RESET;

      Rec := PackageLineTmp;

      CurrPage.UPDATE(FALSE);
    END;

    BEGIN
    END.
  }
}

