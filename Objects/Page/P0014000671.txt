OBJECT Page 14000671 Select Source Document
{
  OBJECT-PROPERTIES
  {
    Date=06/28/17;
    Time=12:00:00 PM;
    Version List=SE0.54.10;
  }
  PROPERTIES
  {
    CaptionML=ENU=Select Source Document;
    InsertAllowed=No;
    DeleteAllowed=No;
    SourceTable=Table14000711;
    PageType=List;
    OnFindRecord=BEGIN
                   FastPackLineTmp := Rec;
                   IF NOT FastPackLineTmp.FIND(Which) THEN
                     EXIT(FALSE);
                   Rec := FastPackLineTmp;
                   EXIT(TRUE);
                 END;

    OnNextRecord=VAR
                   CurrentSteps@1240030001 : Integer;
                 BEGIN
                   FastPackLineTmp := Rec;
                   CurrentSteps := FastPackLineTmp.NEXT(Steps);
                   IF CurrentSteps <> 0 THEN
                     Rec := FastPackLineTmp;
                   EXIT(CurrentSteps);
                 END;

    OnModifyRecord=BEGIN
                     SelectedQuantityBase :=
                       SelectedQuantityBase + "Qty. to Pack (Base)" - xRec."Qty. to Pack (Base)";

                     FastPackLineTmp := Rec;
                     FastPackLineTmp.MODIFY;
                     EXIT(FALSE);
                   END;

  }
  CONTROLS
  {
    { 1900000001;0;Container;
                ContainerType=ContentArea }

    { 17  ;1   ;Field     ;
                CaptionML=ENU=Quantity to Pack;
                DecimalPlaces=0:5;
                SourceExpr="Qty. per Unit of Measure" * InputQuantityBase;
                Editable=FALSE }

    { 19  ;1   ;Field     ;
                CaptionML=ENU=Quantity Selected;
                DecimalPlaces=0:5;
                SourceExpr="Qty. per Unit of Measure" * SelectedQuantityBase;
                Editable=FALSE }

    { 1   ;1   ;Group     ;
                GroupType=Repeater }

    { 2   ;2   ;Field     ;
                SourceExpr="From Source ID";
                Editable=FALSE }

    { 4   ;2   ;Field     ;
                CaptionML=ENU=Source Description;
                SourceExpr=PackingControl.FormatSource("Source Type","Source Subtype","From Source ID");
                Editable=FALSE }

    { 6   ;2   ;Field     ;
                SourceExpr="Qty. to Ship (Base)";
                Editable=FALSE }

    { 8   ;2   ;Field     ;
                SourceExpr="Qty. Packed (Base)";
                Editable=FALSE }

    { 10  ;2   ;Field     ;
                SourceExpr="Remaining Qty. to Pack (Base)";
                Editable=FALSE }

    { 15  ;2   ;Field     ;
                SourceExpr="Qty. to Pack" }

  }
  CODE
  {
    VAR
      FastPackLineTmp@1240030000 : TEMPORARY Record 14000711;
      PackingControl@1240030001 : Record 14000717;
      InputQuantityBase@1240030002 : Decimal;
      SelectedQuantityBase@1240030003 : Decimal;

    PROCEDURE LoadFastPackLines@2(VAR NewFastPackLineTmp@1240030000 : TEMPORARY Record 14000711;NewInputQuantityBase@1240030001 : Decimal);
    BEGIN
      SelectedQuantityBase := 0;

      FastPackLineTmp.RESET;
      FastPackLineTmp.DELETEALL;
      IF NewFastPackLineTmp.FIND('-') THEN
        REPEAT
          FastPackLineTmp := NewFastPackLineTmp;
          FastPackLineTmp.INSERT;

          SelectedQuantityBase := SelectedQuantityBase + FastPackLineTmp."Qty. to Pack (Base)";
        UNTIL NewFastPackLineTmp.NEXT = 0;

      InputQuantityBase := NewInputQuantityBase;
    END;

    PROCEDURE GetFastPackLines@1(VAR NewFastPackLineTmp@1240030000 : TEMPORARY Record 14000711);
    BEGIN
      NewFastPackLineTmp.RESET;
      NewFastPackLineTmp.DELETEALL;

      FastPackLineTmp.RESET;
      IF FastPackLineTmp.FIND('-') THEN
        REPEAT
          NewFastPackLineTmp := FastPackLineTmp;
          NewFastPackLineTmp.INSERT;
        UNTIL FastPackLineTmp.NEXT = 0;
    END;

    BEGIN
    END.
  }
}

