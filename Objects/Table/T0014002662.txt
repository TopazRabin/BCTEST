OBJECT Table 14002662 Sporadic Rule
{
  OBJECT-PROPERTIES
  {
    Date=03/27/19;
    Time=12:00:00 PM;
    Version List=UBP3.00;
  }
  PROPERTIES
  {
    OnDelete=BEGIN
               AdvForSetup.GET;
               IF AdvForSetup."Def. Sporadic Rule" = Code THEN
                 ERROR(Text001,Code,AdvForSetup.TABLECAPTION);

               Location.RESET;
               Location.SETRANGE("Sporadic Rule",Code);
               IF Location.FIND('-') THEN
                 ERROR(Text002,Code,Location.TABLECAPTION);

               ProcUnit.RESET;
               ProcUnit.SETRANGE("Sporadic Rule",Code);
               IF ProcUnit.FIND('-') THEN
                 ERROR(Text002,Code,ProcUnit.TABLECAPTION);

               SporadicUsageLTMult.RESET;
               SporadicUsageLTMult.SETRANGE("Sporadic Code",Code);
               SporadicUsageLTMult.DELETEALL;

               SporadicUsagePerMult.RESET;
               SporadicUsagePerMult.SETRANGE("Sporadic Code",Code);
               SporadicUsagePerMult.DELETEALL;
             END;

    CaptionML=ENU=Sporadic Rule;
    LookupPageID=Page14002719;
    DrillDownPageID=Page14002719;
  }
  FIELDS
  {
    { 1   ;   ;Code                ;Code10        ;CaptionML=ENU=Code;
                                                   NotBlank=Yes }
    { 11  ;   ;Description         ;Text40        ;CaptionML=ENU=Description }
    { 12  ;   ;Lead Time Mult. Exists;Boolean     ;FieldClass=FlowField;
                                                   CalcFormula=Exist("Sporadic Usage L.T. Mult." WHERE (Sporadic Code=FIELD(Code)));
                                                   CaptionML=ENU=Lead Time Mult. Exists;
                                                   Editable=No }
    { 13  ;   ;Periods of Average Usage;Integer   ;OnValidate=VAR
                                                                InvMgtSetup@1240020000 : Record 14000551;
                                                              BEGIN
                                                                IF "Periods of Average Usage" > 0 THEN BEGIN
                                                                  InvMgtSetup.GET;
                                                                  IF "Periods of Average Usage" > InvMgtSetup."No. of Periods in a Year" THEN
                                                                    ERROR(Text003, Code);
                                                                END;
                                                              END;

                                                   CaptionML=ENU=Periods of Average Usage }
  }
  KEYS
  {
    {    ;Code                                    ;Clustered=Yes }
  }
  FIELDGROUPS
  {
  }
  CODE
  {
    VAR
      AdvForSetup@1240030002 : Record 14002651;
      Location@1240030003 : Record 14;
      ProcUnit@1240030004 : Record 14000555;
      SporadicUsageLTMult@1240030000 : Record 14002660;
      SporadicUsagePerMult@1240030001 : Record 14002661;
      Text001@1240030006 : TextConst 'ENU=Stocking Rule %1 exists in %2';
      Text002@1240030005 : TextConst 'ENU=Stocking Rule %1 exists in at least one %2';
      Text003@1240020000 : TextConst 'ENU=Stocking Rule %1 periods to average cannot exceed the periods in a year specified on the inventory managment setup.';

    BEGIN
    END.
  }
}

